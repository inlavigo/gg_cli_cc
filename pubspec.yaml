name: aud_cli_gc
description: Audanika Developer Command Line - Generate Code With Ease, e.g. Classes
version: 1.0.0
repository: https://github.com/inlavigo/aud_cli_gc

environment:
  sdk: ^3.4.0-120.0.dev

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  args: ^2.4.2
  coverage: ^1.7.2
  lints: ^3.0.0
  pana: ^0.22.0
  test: ^1.24.0
  yaml: ^3.1.2
